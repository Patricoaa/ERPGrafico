POSTGRES_DB=nombre_de_tu_db
POSTGRES_USER=tu_usuario
POSTGRES_PASSWORD=tu_password
DJANGO_SECRET_KEY=genera-uno-con-python-secrets
NEXT_PUBLIC_API_URL=http://localhost/api

# TUU Remote Payment — ADR 002
# En producción: TUU_GATEWAY_MODE=live + TUU_ENCRYPTION_KEY=<fernet-key>
# Generar clave Fernet: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TUU_GATEWAY_MODE=fake
TUU_ENCRYPTION_KEY=